# Dstatus

## Keymaps

`c` - Commit changes in this container to a new image.
      The image tag will be <container_name>:latest.
`K` - Stop and kill this container.
`l` - `tail -f` the logs on this container.
`p` - Patch current dir with results of `git diff` inside the container.
`r` - Refresh the :Dstatus preview.
`s` - Drop into a shell on this container.

`<backspace>` - Delete this container.
`?` - Toggle the help dialog.
`q` - Quit. Close this preview dialog.
